#include "main.h"
#include <string.h>
/**
*_strlen - return the total length of a string.
*@s: the string.
*
*
*Return: returns the length of the string.
*/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
