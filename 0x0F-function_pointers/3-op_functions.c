/**
 * op_add - adds two interger together.
 * @a: fist number
 * @b: second number
 * Return: sum of a and b.
 */
int op_add(int a, int b)
{
	return (a + b)
}

/**
 * op_sub - subtracts two interger.
 * @a: fist number
 * @b: second number
 * Return: difference of a and b.
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiplies two interger.
 * @a: fist number
 * @b: second number
 * Return: product of a and b.
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divides two interger.
 * @a: fist number
 * @b: second number
 * Return: division of a by b.
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - remainder value of two interger.
 * @a: fist number
 * @b: second number
 * Return: remainder of division of a by b.
 */

int op_mod(int a, int b)
{
	return (a % b);
}
