#include <stdio.h>
/**
 * main - Entry point
 * Description - prints the file name it was compiled in.
 *
 * Return: return zero (success)
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
